import IconButton from "../../components/IconButton"
import { MenuIcon } from "./Icons"

export default function Menu() {
  const handleClick = () => {
    
  }

  return (
    <IconButton>
      <MenuIcon />
    </IconButton>
  )
}